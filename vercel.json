{
  "buildCommand": "cd apps/web && npm install --ignore-scripts && npm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install --ignore-scripts",
  "framework": "nextjs"
}
